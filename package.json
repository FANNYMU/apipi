{
  "name": "apipi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "yarn build && node dist/examples.js",
    "start": "node dist/examples.js",
    "build": "npx tsc --outDir dist",
    "clean": "rm -rf dist",
    "clean:all": "rm -rf node_modules dist package-lock.json yarn.lock"
  },
  "dependencies": {
    "@types/node": "^24.0.14",
    "axios": "^1.10.0",
    "cheerio": "^1.1.0",
    "execa": "^9.6.0",
    "typescript": "^5.8.3"
  }
}
